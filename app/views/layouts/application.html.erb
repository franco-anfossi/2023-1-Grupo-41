<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
    <script defer src="https://use.fontawesome.com/releases/v5.14.0/js/all.js"></script>
    <title>Proyecto</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .media-content {
        overflow: hidden;
      }
    </style>
  </head>

  <body class = "has-text-white has-text-right has-background-black">
    <% if user_signed_in? %>
      <div class="custom-container">
        <div class="card" style="width: 200px; max-width: none; margin-left: auto; margin-right: 1px; height: 80px; background-color: hsl(171, 100%, 41%);">
          <div class="card-content" style="overflow-x: auto;">
            <div class="media">
              <div class="media-left">
                <figure class="image is-48x48">
                  <img src="<%= current_user.imagen.url(:width=>60, :crop=>"fill") %>" alt="Placeholder image">
                </figure>
              </div>
              <div class="media-content">
                <p class="title is-4" style="color: #fff; white-space: nowrap; overflow: hidden;"><%= link_to current_user.name, edit_user_registration_path%></p>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
